@using MyPortfolio.Models.Entities
@model MyPortfolio.Models.Entities.Coffee;

<div class="card" style="min-width: 18rem;">
    <div class="item-image">
        <a asp-action="Details" asp-controller="CoffeeShop" asp-route-id="@Model.Id" class="card-img-top images">
            <img src="@Model.ImagePath" class="card-img-top images" style="object-fit: contain">
        </a>
    </div>
    <div class="card-body">
        <h5 class="card-title">@Model.Name</h5>
        <p class="card-text">@Model.Price تومان</p>
        @if (Model.Type == Category.Hot)
        {
            <span class="card-text">نوع قهوه: گرم</span>
        }
        else 
        {
            <span class="card-text">نوع قهوه: سرد</span>
        }
        <br />
        <br />
    </div>
</div>